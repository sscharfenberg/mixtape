*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# --- MixTape (added) ---
# TypeScript incremental-build cache (emitted by `vue-tsc --build`) — machine-local, regenerated on every build.
*.tsbuildinfo
# Local-only / host-specific docs (e.g. docs/debbie-infrastructure.local.md) — never commit.
*.local.md
# Claude Code local/personal settings.
.claude/settings.local.json

# Home-infrastructure docs — never tracked (see CLAUDE.md -> Docs).
#
# This repo is public. docs/debbie.local/ holds everything specific to the one
# live server: LAN topology, MACs, the DynDNS host, the real domain, secret
# LOCATIONS. The generic, reproducible version of the same material is
# docs/self-hosting/, which IS tracked and uses placeholders throughout.
#
# The trailing slash ignores the whole directory. Note the *.local.md rule above
# would NOT cover files inside it (infrastructure.md does not match that glob),
# so this entry is doing real work — do not remove it.
docs/debbie.local/
